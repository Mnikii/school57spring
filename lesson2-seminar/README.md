# Домашнее задание: Первые шаги в Spring



Это командное задание. Оценка выставляется на основании работоспособности кода и **равномерности распределения задач** между участниками (проверяется через историю коммитов в GitHub).



---



### 1. Подготовка и Git

* **Репозиторий:** Создайте один общий репозиторий на GitHub для всей команды.

* **Инициализация:** Сгенерируйте скелет проекта через [start.spring.io](https://start.spring.io).

* **Пример для работы:** Структура должна быть аналогична нашему семинару: [GitHub Lesson 2](https://github.com/rkarabash/school57spring/tree/master/lesson2-seminar).



### 2. Разработка бизнес-логики

1. **Выбор сущности:** Выберите одну или несколько сущностей из предметной области **вашего командного проекта**.

2. **Сервис и CRUD:** Реализуйте сервис, который выполняет стандартные операции (Create, Read, Update, Delete) над простой коллекцией (`List` или `Map`) ваших сущностей.

3. **Конфигурирование (Усложнение):** * Добавьте в `application.yaml` настройки для работы сервиса.

    * **Логика:** Сервис должен считывать эти параметры (через `@Value` или `@ConfigurationProperties`) и менять свое поведение.

    * *Например: список запрещенных имен, лимит на количество записей или фильтр по категориям.*



### 3. Демонстрация работы

* **CommandLineRunner:** Создайте класс, который автоматически запускается при старте приложения.

* **Показательные логи:** Внутри `run` вызовите все методы сервиса. Каждое действие должно сопровождаться подробным логом в консоли.

* **Важно:** В логах должно быть четко видно:

    * Успешное выполнение операций.

    * Работу конфигурации (например, когда создание объекта отклонено из-за попадания в «черный список»).



---



### 📅 Дедлайн

**2 февраля, 23:59**



---

**Удачи в разработке!**

